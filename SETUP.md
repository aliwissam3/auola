# إعداد وتشغيل صيدليتي (Pharmacy Manager)

هذا المجلد يحتوي على كود Dart/Flutter الكامل للتطبيق (`lib/`) وملف
الاعتماديات (`pubspec.yaml`). مجلدات المنصّات (`android/`, `ios/`,
`windows/`, `web/`, `macos/`, `linux/`) **غير مولّدة بعد** لأن بيئة
التطوير التي كتبت بها هذا الكود لا تملك Flutter SDK مثبّتاً لتشغيل أدوات
التوليد والبناء. اتبع الخطوات التالية على جهازك لإكمال المشروع وتشغيله.

## 1) تثبيت المتطلبات

- Flutter SDK (قناة stable) — راجع https://docs.flutter.dev/get-started/install
- لأندرويد: Android Studio + Android SDK
- لويندوز: Visual Studio 2022 (حمولة "Desktop development with C++")
- تأكد من:
  ```bash
  flutter doctor
  ```

## 2) توليد مجلدات المنصّات

من داخل هذا المجلد (`pharmacy_manager/`):

```bash
flutter create --project-name pharmacy_manager --org com.yourcompany .
```

هذا الأمر يضيف `android/`, `ios/`, `windows/`, `web/`, `macos/`, `linux/`
بدون أن يلمس `lib/` أو `pubspec.yaml` الموجودين (Flutter يدمج فقط الملفات
الناقصة). راجع الفروقات في `pubspec.yaml` بعد التنفيذ للتأكد أنه لم يستبدل
قسم `dependencies` الذي كتبناه — إن حدث ذلك أعد لصق محتوى `dependencies`
الأصلي.

## 3) تنزيل الحزم

```bash
flutter pub get
```

> ملاحظة: أرقام إصدارات `google_mlkit_text_recognition`، `barcode_widget`،
> `pdf`، و `printing` في `pubspec.yaml` مثبّتة على قيم معقولة وقت كتابة هذا
> الكود. إن رفض `pub get` أياً منها (تعارض إصدارات، كما حصل سابقاً مع
> `intl`)، افتح صفحة الحزمة على pub.dev وحدّث الرقم لآخر إصدار متوافق، ثم
> أعد `flutter pub get`.

## 4) صلاحيات المنصّات (تُضاف يدوياً بعد التوليد)

### أندرويد — `android/app/src/main/AndroidManifest.xml`
أضف داخل وسم `<manifest>` (قبل `<application>`):
```xml
<uses-permission android:name="android.permission.CAMERA"/>
<uses-permission android:name="android.permission.INTERNET"/>
```

### iOS — `ios/Runner/Info.plist`
أضف:
```xml
<key>NSCameraUsageDescription</key>
<string>لالتقاط صور قسائم الشراء والمنتجات</string>
<key>NSPhotoLibraryUsageDescription</key>
<string>لاختيار صور من المعرض</string>
```

### الويب (PWA) — `web/manifest.json`
عدّل `name`, `short_name`, `theme_color`, `background_color` بما يناسب
هوية "صيدليتي"، واستبدل أيقونات `web/icons/` بشعار الصيدلية.

## 5) التشغيل

```bash
# ويندوز (تنفيذي)
flutter run -d windows

# أندرويد (على جهاز/محاكي متصل)
flutter run -d android

# الويب (PWA)
flutter run -d chrome
```

### تشغيل نسخة الويب بحيث يوصلها الموبايل (لميزة "مسح الكود يفتح المتصفح")

فيه طريقتان لهذا — الأولى (يدوية) تصلح لو شغّلت نسخة الويب مباشرة
بمتصفح الأدمن (`flutter run -d chrome`)، والثانية (تلقائية) هي المعتمدة
فعلياً لأن `update_and_run.bat` يشغّل نسخة **ويندوز** لا الويب.

#### الطريقة التلقائية (زر "باركود" بشاشة الدخول) — هذي المستخدمة فعلياً

نسخة الويندوز نفسها تستضيف نسخة الويب من داخلها (`lib/services/local_web_server_service.dart`)
بدون أي خطوة يدوية أثناء التشغيل — بس أول مرة (أو بعد أي تعديل بالكود)
لازم يكون فيه بناء ويب جاهز بمجلد `build/web`:

```bash
flutter build web --release
flutter run -d windows
```

(`update_and_run.bat` المرفق يسوي الخطوتين هاتين تلقائياً بكل مرة —
ما تحتاج تكتبهم يدوياً إذا تستخدمه).

عند فتح البرنامج، زر **"باركود"** بأول شاشة الدخول يفتح QR على عنوان
مثل `http://192.168.1.5:8089` — امسحه بأي موبايل بنفس شبكة الواي فاي.
تأكد إن جدار حماية ويندوز (Windows Firewall) ما يحجب المنفذ `8089`
(أول مرة يشتغل السيرفر، ويندوز غالباً يسأل "Allow access?" — اختر
"السماح" على الشبكات الخاصة/المنزلية).

إذا الزر ما ظهر: يعني `build/web` مو موجود أصلاً (لسا ما سويت
`flutter build web`)، أو الجهاز مو متصل بأي شبكة، أو المنافذ
8089-8091 كلها مشغولة ببرنامج ثاني.

**إذا الزر ظهر والباركود انفتح بالموبايل بس الشاشة طلعت فارغة (بيضاء):**
هذا غالباً لأن Flutter بشكل افتراضي يحمّل محرك عرض (CanvasKit) يجيب
ملفات إضافية من سيرفر Google على الإنترنت — وهذا يفشل بصمت (شاشة فارغة
بدون أي رسالة خطأ) لو الجهاز/الشبكة ما عندها إنترنت فعلي، حتى لو الاتصال
بالسيرفر المحلي نفسه نجح. جرّب تبني بمحرك العرض `html` بدله، وهذا
مستقل تماماً عن الإنترنت:

```bash
flutter build web --release --web-renderer html
```

إذا نجح وطلعت الشاشة صح، عدّل `update_and_run.bat` وضيف
`--web-renderer html` بنفس مكان أمر `flutter build web --release`
فيه حتى يصير تلقائي بكل مرة. (ما ضفتها تلقائياً هنا لأن هذا الخيار
انشال بإصدارات Flutter الأحدث جداً — لو `flutter build web` رفض
العلم هذا برسالة "Could not find an option"، يعني نسختك ما تدعمه
وتجاهل هذه الخطوة، والمشكلة شي ثاني — افتح أدوات المطوّر بمتصفح
الموبايل (أو `chrome://inspect` من جهاز آخر متصل بنفس الكيبل) وشوف
رسالة الخطأ بالـ Console.)

#### الطريقة اليدوية (فتح نسخة الويب مباشرة بمتصفح الأدمن)

1. شغّل السيرفر بحيث يستمع على كل الشبكة، مو بس هذا الجهاز:
   ```bash
   flutter run -d chrome --web-hostname=0.0.0.0 --web-port=8080
   ```
2. لكيت عنوان الـIP المحلي لجهازك (بويندوز):
   ```bash
   ipconfig
   ```
   دوّر على "IPv4 Address" (مثلاً `192.168.1.5`).
3. **افتح متصفح الأدمن نفسه على هذا العنوان** (مو `localhost`):
   ```
   http://192.168.1.5:8080
   ```
   لأن رابط الـQR يُبنى من نفس العنوان اللي فاتح منه المتصفح — إذا فتحته
   بـ `localhost` بيّولد رابط `localhost` ما يشتغل من أي جهاز ثاني.
4. تأكد الموبايل متصل بنفس شبكة الواي فاي متاع الجهاز، وتأكد جدار حماية
   ويندوز (Windows Firewall) ما يحجب المنفذ 8080.

## 6) البناء للإصدار النهائي

```bash
# ملف تنفيذي لويندوز (المجلد الناتج: build/windows/x64/runner/Release)
flutter build windows

# APK لأندرويد
flutter build apk --release

# تطبيق ويب PWA (المجلد الناتج: build/web)
flutter build web --release
```

## بيانات الدخول الافتراضية (بيانات تجريبية تُنشأ تلقائياً أول تشغيل)

- اسم المستخدم: `المدير`
- كلمة المرور: `admin123`
- رمز PIN الطوارئ الافتراضي: `0000` (غيّره من الإعدادات فور أول دخول)

## ملخص ما تم تنفيذه في `lib/`

- `theme/` — 5 باقات ألوان (الملكي، الياقوتي، الكلاسيكي، الزمردي، المكتبي
  النهاري) مع حفظ الاختيار محلياً.
- `data/` — طبقة تخزين محلي عامة (JSON فوق `shared_preferences`، تعمل على
  كل المنصّات دون الحاجة لتوليد أكواد `build_runner`) + دوال مشتقة
  (أرصدة الديون، المخزون المنخفض، الإكسباير القريب).
- `models/` — كل الكيانات: المنتجات والدفعات، الموظفين والصلاحيات،
  المراجعين والديون، الموردين والمندوبين، فواتير البيع والشراء، النقوصات،
  المرتجعات، حركة المخزون، المصاريف، سجل الدخول.
- `screens/` — شاشة تسجيل الدخول، لوحة التحكم، المنتجات (`products` —
  إضافة/تعديل/صورة/كمية جديدة، مستقلة عن شاشة المشتريات)، نقطة البيع
  (`pos`، سلال متعددة)، المشتريات مع OCR (`purchases`)، الديون (`debts`)،
  الموظفين (`employees`)، الإعدادات وباقات الألوان (`settings`)،
  النقوصات (`deficits`)، الإكسباير (`expiry`)، المندوبين والمذاخر
  (`reps` — تفتح صفحة تفصيلية لكل مورد)، كل القوائم (`lists`)، المواد
  المعادة (`returns`)، حركة مادة (`movement`)، التسديد والمصاريف
  (`settlements`)، الدخولات (`entries`)، التقارير (`reports`)، والصيدليات
  (`pharmacies`).
- `services/` — تسجيل الدخول والصلاحيات، PIN الطوارئ، محرك OCR
  (Android/iOS عبر ML Kit، مع استبعاد آمن من بناء الويب عبر conditional
  imports حتى لا يفشل `flutter build web`)، طباعة الفواتير وبطاقات
  الموظفين (PDF عبر `pdf`/`printing`).
- `utils/arabic.dart` — تطبيع الهمزات العربية (أ/إ/آ → ا) مستخدم بكل
  حقول البحث (المنتجات، نقطة البيع، النقوصات، الديون) حتى يلقى المستخدم
  النتيجة حتى لو كتب بدون همزة.

## قيود معروفة يجب مراعاتها

- **OCR**: يعمل فعلياً على أندرويد و iOS فقط (قيود ML Kit). على ويندوز/الويب
  يظهر نموذج إدخال يدوي بعد إرفاق صورة القسيمة. كذلك محرك ML Kit لا يدعم
  التعرف على النص العربي (فقط لاتيني) — القسائم العربية بالكامل تحتاج إدخالاً
  يدوياً.
- **قارئ الباركود**: مبني على حقل نصي يستقبل إدخال قارئ الباركود الفيزيائي
  (USB/Bluetooth) كلوحة مفاتيح، وهو الأسلوب المعتمد فعلياً في الصيدليات. لم
  يُضف مسح بالكاميرا (mobile_scanner) لتبسيط الاعتماديات؛ يمكن إضافته لاحقاً
  بسهولة.
- **طباعة الفواتير وبطاقات الموظفين**: تحتاج اتصال إنترنت **أول مرة فقط**
  على كل جهاز (لتحميل خط عربي من Google Fonts عبر حزمة `printing`، ثم يُحفظ
  محلياً). بدون إنترنت بالمرة الأولى ستفشل الطباعة برسالة خطأ شبكة.
- **صورة المنتج**: تُخزَّن كنص base64 داخل نفس ملف بيانات المنتجات
  (SharedPreferences)، بدون ضغط إضافي غير ما يوفره `image_picker`
  (`maxWidth: 480, imageQuality: 70`). إضافة صور لعدد كبير جداً من
  المنتجات (مئات) قد تُبطئ الحفظ/التحميل — لصيدلية عادية هذا لا يشكل مشكلة.
- **تخزين البيانات محلي فقط** (لا مزامنة سحابية بين الأجهزة) — يناسب صيدلية
  واحدة تعمل من جهاز/عدة أجهزة غير متصلة ببعضها. لإضافة صيدليات متعددة
  متزامنة فعلياً يلزم واجهة خلفية (backend) لاحقاً.

## 6) شعار التطبيق (أيقونة النافذة/exe بدل شعار Flutter الافتراضي)

مضاف صورة شعار جاهزة بـ `assets/icon/app_icon.png` وحزمة `flutter_launcher_icons`
بـ `pubspec.yaml`. بعد `flutter create .` و `flutter pub get`، نفّذ:

```bash
dart run flutter_launcher_icons
```

هذا يستبدل شعار Flutter الافتراضي بأيقونة الصيدلية على كل المنصات
(Windows exe، أيقونة أندرويد، تبويب الويب...) تلقائياً. إذا حبيت تغيّر
الشعار لاحقاً، استبدل نفس ملف `assets/icon/app_icon.png` بصورة جديدة (مربعة،
1024×1024 يفضّل) وأعد تنفيذ نفس الأمر.

## 7) صلاحية الكاميرا لمسح الباركود بالموبايل

ملف `AndroidManifest.xml` الجاهز (بصلاحية الكاميرا مضافة مسبقاً) موجود ضمن هذا المشروع
بالمسار `android/app/src/main/AndroidManifest.xml` — ما تحتاج تعدل شي يدوياً.

⚠️ **ترتيب مهم فقط أول مرة تبني فيها للأندرويد:**
1. أول `flutter create --project-name pharmacy_manager --org com.aliwissam .` (لو ما سويتها من قبل — تصنع مجلد android بالكامل بالافتراضي)
2. **بعدها** انسخ مجلد `android` من هذا الملف فوق مشروعك (استبدال) — حتى يرجع ملف الصلاحية المُجهّز يستبدل النسخة الافتراضية
3. `flutter build apk --release`

إذا سويت `flutter create .` مرة ثانية بعدين لأي سبب، لازم تعيد نسخ مجلد `android` من هذا الملف فوقه من جديد،
وإلا يرجع الملف الافتراضي بدون صلاحية الكاميرا.

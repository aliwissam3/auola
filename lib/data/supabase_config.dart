/// Supabase project connection details for this pharmacy's cloud sync.
///
/// The publishable key is meant to be public (it's baked into every
/// client app, same as any "anon" key) — it only grants what the
/// database's Row Level Security policies allow, so it's safe to ship
/// inside the app.
class SupabaseConfig {
  static const url = 'https://tdzpunjmqfytcpwoadxr.supabase.co';
  static const publishableKey = 'sb_publishable_1NautTl-OwWbeQ-E15_FRg_uHqd2BoZ';
}
